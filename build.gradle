plugins {
    id 'java'
    id 'maven-publish'
}

group 'org.voyager'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.vigneshwaran'
            artifactId = 'voyager'
            version = '1.0.0'

            from components.java
        }
    }
}

dependencies {
    implementation 'org.seleniumhq.selenium:selenium-java:4.22.0'
}